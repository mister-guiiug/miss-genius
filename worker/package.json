{
  "name": "miss-genius-pronote-proxy",
  "private": true,
  "type": "module",
  "description": "Proxy Cloudflare Worker : se connecte à Pronote via pawnote et renvoie les notes normalisées (contourne CORS pour le PWA Miss Genius).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pawnote": "^1.6.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260926.1",
    "typescript": "~6.0.3",
    "wrangler": "^4.141.0"
  }
}
